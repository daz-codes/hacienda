<% page_title "Regenerate #{workout.activity}" %>

<section class="form-card">
  <p class="eyebrow">Training log</p>
  <h1>Regenerate workout</h1>
  <p class="lede">Change the brief and create a fresh set of difficulty variants.</p>

  <%= partial :form, workout:, errors:, context: %>
</section>
